#!/bin/bash

# absolute path to the wiki directory
wikidir="/home/dirk/Projects/Private/wiki"

if [ ! -d $wikidir ]; then
    echo "The given wiki directory '$wikidir' doesn't exist!"
    exit 1
fi

cd $wikidir
./wiki -s
